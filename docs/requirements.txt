# Requirements for building the Sphinx documentation (also used by ReadTheDocs;
# see .readthedocs.yml). Keep in sync with the `docs` extra in pyproject.toml.
#
# Runtime packages that autodoc has to import while documenting ggnomics must
# be listed here as well.
furo
ipykernel
myst-parser[linkify]
nbsphinx
numpy
pandas
plotnine>=0.16.0a11,<0.17
sphinx>=7.2
sphinx-autodoc-typehints
sphinx-copybutton
